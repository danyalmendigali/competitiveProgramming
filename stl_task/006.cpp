#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define array arr

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    vector <int> myVector;
    myVector.push_back(3);
    myVector.push_back(12);
    myVector.push_back(45);

    for(auto e : myVector) {
        cout << e << endl;
    }







   return 0;
}
