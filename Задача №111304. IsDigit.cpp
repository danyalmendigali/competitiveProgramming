#include <iostream>
using namespace std;

bool isDigit(char s) {
	if(s + 0 >= 48 && s + 0 <= 57) cout << "YES";
	else cout << "NO";
}
int main() {
	char s;
	cin >> s;
	isDigit(s);
	
	
	
	
	
	
	
	return 0;
}
