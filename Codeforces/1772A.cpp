#include <set>
#include <cmath>
#include <vector>
#include <string>
#include <cstring>
#include <iostream>
#include <algorithm>

using namespace std;

int main() {
	int n;
	cin >> n;
	for(int i = 0; i <= n; i++) {
		string s;
		cin >> s;
		for(int j = 0; j < s.size(); i++) {
			if(s[j] >= '0' && s[j] <= '9' ) {
				int a;
				a += s[j];
			}
		}
	}
	
	
	
	
	
	return 0;
}
