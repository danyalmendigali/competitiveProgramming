#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

void solve()
{
    long long int x, y, z;
    cin >> x >> y >> z;

    cout << (x * 3) + (y * 5) + (z * 12);
}

int main() {

    solve();


    return 0;
}
