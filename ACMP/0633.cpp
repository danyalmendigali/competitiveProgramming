#include <iostream>
#include <string>
#include <algorithm>
using namespace std;
#define N 3

int main() {
	string name_team;
	string team[N];
	
	getline(cin, name_team);
	for(int i = 0; i < N; i++) {
		getline(cin, s[i]);
	}
	
	sort(s, s + N);
	cout << name_team << ", " << team[0] << ", " << team[1] << ", " << tea
	
	
	
	return 0;
}
