#include <iostream>
using namespace std;

int main() {
	long long int a, b, c, a1, b1, c1;
	cin >> a >> b >> c;
	a1 = a / 2;
	b1 = b / 2;
	c1 = c;
	cout << min(a1, min(b1, c1));
	
	
	
	
	
	
	return 0;
}
